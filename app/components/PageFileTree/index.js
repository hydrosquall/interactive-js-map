// A component to visualize the filesystem as a Dagre layout based tree
// Can use this as a scaffolding upon which to layer data.
import PageFileTreeContainer from './PageFileTree.container';

export default PageFileTreeContainer;
